#pragma once

#include <iostream>
#include <string>
#include <vector>

using std::string;
using std::size_t;
using std::vector;

class ConvertedFile {

    private:

        size_t type;
        string description;
        size_t max_value;
        vector<vector<size_t>> grid;

    public:

        

};